/*
 * UpdateStudentModal Messages
 *
 * This contains all the text for the UpdateStudentModal component.
 */

import { defineMessages } from 'react-intl'

export const scope = 'app.components.UpdateStudentModal'

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UpdateStudentModal component!',
  },
})
